# 2026-07-26

## H5 构建体积告警处理
- `npm run build:h5`（Taro v4.1.11）原本报 webpack 性能告警：`js/app.js 266KiB`、`app` 入口 401KiB 略超默认 244KiB 推荐值，属正常体积、不影响产物。
- 处置：在 `client/config/index.ts` 的 `h5.webpackChain` 加 `chain.performance.maxAssetSize(512KiB).maxEntrypointSize(512KiB)` 抬高预算阈值，告警消失，构建干净。未动业务代码，可删段回滚。
- 若日后想真缩包可走 splitChunks 拆 vendor / 路由懒加载 / 图片压缩（当时定为"方案2"，用户未要求）。

## 恢复合规被砍功能（独立 APK/Web 迁移前提下）
- 用户要求加回两项「心血」：① 人机对决排行榜；② AI 预测结果公开展示 + 原来的分析文案。判定这两项为体育数据+社区功能，属合规安全区（不涉及真钱下注闭环）。
- 已恢复（均从 git HEAD 还原，因合规改动多为未提交的 working-tree 删除/改写）：
  - `client/src/pages/leaderboard/index.tsx`：还原 human（人机对决）tab + UserAvatar/resolveAvatarUrl + 人类排行渲染（整文件 `git checkout HEAD`）。
  - `client/src/pages/vote-history/*`：整页 3 文件从 HEAD 还原（之前被删）。
  - `client/src/config/features.ts`：`VOTE: false → true`（人机对决依赖投票数据来源；注释原本就写"重新开放把开关改 true"）。
  - `client/src/app.config.ts`：加回 `pages/vote-history/index` 路由 + 标题还原为「AI 预测世界杯」。
  - `match-detail/index.tsx`、`ai-detail/index.tsx`：逐处还原被洗白的"分析↔预测、占优↔胜"措辞（主胜/客胜/平局、AI 预测对战卡/AI 预测世界杯 标题、AI 预测尚未生成/多模型智能预测、预测失误 标签、解锁 AI 预测 等）；H5 构建通过，lint 0 错误。
- 边界（重要）：原版 HEAD 有一句带「投注」的免责（match-detail L329 `不构成投注建议`，以及 risk-reminder `投注`/`理性娱乐」），**未恢复**——「投注/下注」仍在项目敏感词禁用清单（见 MEMORY.md 去微信化段）。当前页脚已是更合规的「仅供学术研究与内容参考 / 不构成任何投资或决策建议」免责，保留不动。段落标题「📊 AI 综合分析」原版本就如此，非合规改动，保持。
- 提醒：离开微信只绕开 6.1.5 平台规则；真·下注闭环、付费卖"保证命中预测"在国内法/应用商店仍高危，未恢复、不建议恢复。
- **踩坑（17:19）**：用户截图反馈 match-detail 的 AI 卡片**没有比分预测和信心指数**。排查发现不是 JSX 缺失（`pred-score-area`/`pred-conf`/`conf-bar` 都在），而是 `client/src/pages/match-detail/index.scss` 第 323-330 行有一组合规期 `display:none`（注释明写"合规：隐藏 AI 综合分析与单模型卡片中的比分、信心指数、进度条、备选参考"），覆盖 `.pred-score-area`、`.pred-card .pred-conf`、`.summary-card .pred-conf`、`.pred-alt-score` 等。**结论**：合规整改不仅改文案，还会改 SCSS 把"预测数据可视化"直接藏起来。下次恢复时不能只看 JSX 措辞，必须顺带审 SCSS。已删除该隐藏块，H5 构建通过，AI 卡片恢复显示 大字比分 + 信心指数 N/10 + 进度条 + 备选参考，综合区信心也恢复。
- **APK 图标（17:36）**：用户把 logo 放 `client/src/assets/logo.jpg`（1408×768 青绿圆徽标）。原 manifest 引用占位 vector `@drawable/ic_launcher`（黑底白圈）。用 Pillow 裁中央正方形 768×768 + 白转透明 + 缩放 48/72/96/144/192 写入 `mipmap-*` 5 个目录，manifest 改 `@mipmap/ic_launcher`。透明背景在 launcher 圆/圆角遮罩下干净。用户需重新 `gradle assembleDebug`（或 Android Studio Build）出 APK 生效。
- **APK 图标白边（21:00）**：用户截图显示 launcher 把透明 PNG 当白底填成「白色圆角矩形 + logo」（应用抽屉/设置预览位不套圆遮罩时）。**根因**：透明 PNG 无法根治，launcher 行为各异。**解法**：升级为 Android adaptive icon（API 26+）。脚本一次产出：① `drawable-nodpi/ic_launcher_foreground.png`（432×432 透明 logo） ② 5 个传统 mipmap PNG 改为**不透明青绿底**（中央 200×200 自动采样色 `#64ACAA`）+ logo 居中 fit  ③ `values/colors.xml` + `drawable/ic_launcher_background.xml`（纯色 shape） ④ `mipmap-anydpi-v26/ic_launcher.xml` + `ic_launcher_round.xml`（adaptive spec）。manifest 不变（仍 `@mipmap/ic_launcher`），系统按 API 自动选 adaptive 或兜底 PNG。
- **圆内白字误删修复（21:13）**：用户指出初版前景 PNG 把圆内白色文字也抠掉了（不满足"不要改圆形里面内容"）。改用**按行圆形弦遮罩**（每行找圆左右非白边界，只保留弦内像素），圆内原样保留、仅圆外变透明。脚本 `make_icons_v3.py` 重生成 `ic_launcher_foreground.png`(143KB，白字/足球/电路全在) 与 mipmap（顺手用 alpha_composite 去白环）。已交付预览，待用户确认。

## 个人中心页改造（17:28）
- 用户截图要求：① 点击登录/头像提示"暂时不支持登录功能"；② 隐藏"分享给好友"；③ 下方空白区放收款码。
- 已修改 `client/src/pages/profile/index.tsx` + `index.scss`：
  - `handleReLogin` 与已登录展示模式头像点击统一 toast 提示"暂时不支持登录功能"。
  - 从 `MENU_ITEMS` 移除「分享给好友」菜单项，并删除对应的 `Button openType='share'` 渲染与 `useShareAppMessage`/`useShareTimeline` 钩子。
  - 新增收款码区域：导入 `client/src/assets/givememoney.png`，在菜单下方用 `Image mode='widthFix'` 展示，配文"欢迎各位大佬打赏一下"；SCSS 加 `.prof-donate` / `.prof-donate-img` / `.prof-donate-tip`。
- 顺手清理了本次改动导致的未使用 `login` 解构；并修复了 VIP 标签处一个遗留的多余 `}` JSX 语法错误。
- lint 0 错误。
