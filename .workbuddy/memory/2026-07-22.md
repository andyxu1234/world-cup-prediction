# 2026-07-22 工作日志

## 20:58 — 新建公开 web 首页 /home（展示 AI 预测，放弃微信分发）
- 用户决策：① 放弃微信分发，做成浏览器直开，后续单独做注册登录；② 改造比赛列表，允许用户看到 AI 预测内容。
- 后端无需改动：`/api/v1/matches`(支持 status/date/sort_order=desc/limit)、`/matches/{id}`(含 predictions[] 每模型 result/score/confidence/analysis)、`/matches/stats`、`/leagues` 均已公开，CORS `*`。
- 新增文件：`server/app/static/public/{index.html, home.js, home.css}`；`main.py` 挂载 `/home` → `static/public` 且根 `/` 重定向到 `/home/`。
- 前端：Hero 统计 + 联赛下拉 + 状态 Tab（全部/未开赛/进行中/已结束，对应 MatchStatus）+ 比赛卡片（联赛/时间/主客队国旗/状态/比分）+ 点击「查看 AI 观点」懒加载展开每场 AI 预测（聚合多数观点 + 每模型胜平负/比分/置信度/分析）。
- 合规：不展示赔率/博彩公司/投注，UI 文案用「AI 观点/模型解读」，底部固定免责声明（不构成投注/购彩建议）。
- 校验：`node --check home.js` 与 `py_compile main.py` 均通过（ALL_OK）。
- 待办：后续单独做注册登录功能（用户明确）。

## 20:40 — Brainstorm：web 比赛列表改造为对外首页（小程序被封后）

- **突发背景**：微信小程序因 6.1.5 违规被封，用户要把 `/internal/odds` 的「比赛列表」tab 改成面向用户的首页。
- **顶层约束**：合规是底线。即使上 web，违规内容仍会被腾讯通过 H5/公众号/域名/服务器通道封禁；新站必须用新主体域名，避免旧案底连坐。
- **现比赛列表雷点扫描**（`odds.js:222-246 renderMatchTable`）：赔率最新快照数字、博彩公司名、AI 模型（跨页可见）、"查看赔率"按钮、整个"投注回测"tab。
- **4 个产品方案**：
  - A. 纯只读资讯版（最稳，无 AI）
  - B. AI 观点版（推荐，展示聚合 AI 倾向，不出具体数字/单模型）
  - C. 完整复刻小程序（❌ 必封）
  - D. toB API 输出（彻底换方向）
- **推荐 B**：保留 10 个 AI 模型资产价值，文案替换为"AI 倾向/模型共识/专家洞察"，加免责声明。
- **技术路径**：新建独立路径 `/picks` + 公开 API `/api/v1/public/matches`（**不能直接拿掉 token**），字段剥离 `odds.*`，新增 `ai_tendency`(home/draw/away/divided) + `ai_confidence`(low/medium/high，不出数字）。
- **待用户决策 4 点**：① 选 A/B/D ② AI 粒度（聚合/分布/不展示） ③ 是否要账户系统 ④ 域名/主体策略（复用/新购/海外托管）。
- **未动代码**，等用户拍板再进 Plan。

## 15:06 — 简化 prompt 赔率段：只取 bet365(id=319) 最新快照

- 用户要求：赔率逻辑不用搞复杂，prompt 里直接用数据库里 bet365（bookmaker_id=319）最新快照即可。
- 改 `server/app/utils/prompt_builder.py` 的 `_format_odds`：删除原"多公司均值 + 剔除抽水隐含概率 + 赔率变动趋势"整套逻辑，改为：
  - 仅筛选 `bookmaker_id==319` 且 `odds_type=prematch` 且 `market='Full Time Result'` 的行；
  - 取最新 `snapshot_date`；
  - 直接输出 主胜/平局/客胜 三项 bet365 赔率（两位小数字），无均值/无隐含概率/无趋势。
- 新增模块级常量 `BET365_ID = 319`；删掉不再使用的 `from collections import defaultdict` 导入；prompt 段标题由"主流公司市场共识盘口"改为"bet365 最新盘口"。
- `_normalize_outcome` 保留（新逻辑仍用它把 Home/Draw/Away 映射到 home/draw/away）。
- 验证：`stub app.database` + `pymysql` 直连真实样本跑通，输出 `bet365 最新赔率（快照 xxxx）：主胜 x.xx / 平局 x.xx / 客胜 x.xx`。lint 0 报错。
- **未改动** `server/app/services/odds_service.py`（仍每日同步 10 家博彩公司落库）。用户只要求"取数"简化，未要求停拉其他公司；如需只同步 bet365 以省 API 配额，可后续再议。

## 14:58 — 实跑 build_user_prompt 验证新 prompt（真实 DB 样本）

- 用注入 stub `app.database`（绕过 Windows 缺失的 asyncmy 异步引擎）+ `pymysql` 直连 47.101.167.103 取真实样本，跑 `app/utils/prompt_builder.build_user_prompt`。
- 样本1 世界杯 Final：Spain vs Argentina。season_stats 诊断确认 `leagueId=1635 'World Cup' 2026 <<< MATCH` 命中，prompt 正确显示世界杯赛季统计，**未退化**到友谊赛/资格赛。
- 样本2 欧冠 2nd Qualifying：Levski Sofia vs U Craiova。诊断确认 `leagueId=2486 'UEFA Champions League' 2026 <<< MATCH` 命中（靠 highlightly_league_id 精确匹配，"欧冠"中英文不匹配但 ID 能命中），未退化到国内联赛。
- 结论：**season_stats 匹配逻辑当前数据下工作正常，无退化风险，无需为匹配微调**（前提是 teams.season_stats 含该联赛条目且 leagues 表 highlightly_league_id 正确）。
- 赔率共识盘口 + 隐含概率（剔除抽水）正确；仅 1 个快照时无"赔率变动趋势"（符合预期）。
- 发现两个数据源问题（非 prompt 逻辑 bug）：
  1. **H2H 脏数据**：Spain vs Argentina 交锋 score 字段为字符串 `"None"`，导致 `_format_head_to_head` 无法解析，显示为 `2026-03-27 Spain None Argentina` 且胜负统计 0:0:0。属 h2h_sync 落库问题。
  2. **FIFA排名拼接 group_name 冗余**：prompt 显示 `FIFA排名 #2小组 H`（fifa_rank=2 拼 group_name="H"），对淘汰赛阶段是误导标签。

## 14:34 — match-detail 恢复投票/广告解锁机制（开关隐藏）

- 用户要求：之前整改把 match-detail 的投票区、广告解锁（激励视频三态机 cinema/locked/unlocked）、VIP 提示全删了改成只读。现希望**恢复代码但用开关隐藏**，后续可打开。
- 新建 `client/src/config/features.ts`：`FEATURES = { VOTE: false, AD_UNLOCK: false }`，默认全关。
- 重写 `client/src/pages/match-detail/index.tsx`：从 git HEAD 版本（587 行，含投票+广告解锁）恢复全部逻辑，但用开关包裹：
  - `AD_UNLOCK` 关闭时 `adPhase` 初值 `'unlocked'`，广告 effect 首行短路 `setAdPhase('unlocked'); return`，不创建广告实例；summary/preds 直接展示；vip-hint 与 cinema/locked 分支仅 `AD_UNLOCK` 为真时渲染。
  - `VOTE` 独立开关：投票区 `FEATURES.VOTE && (...)`。
  - 合规措辞同步改：广告文案"AI 预测"→"AI 分析"，校验提示"主胜/平局/客胜"→"主队占优/势均力敌/客队占优"。
- 样式 `.vote-card`/`.ad-cinema-wrap`/`.ad-lock-wrap`/`.vip-hint` 等 scss 一直没删，直接复用。
- 验证：开关全关时表现等同整改后只读版；lint 0 报错；store 有 `isVip`/`vipExpireAt`。
- 注意：信息流广告 `<ad unit-id={AD_UNIT_IDS.feed}>` 始终渲染（不在本次恢复范围，原只读版也有）。

## 14:29 — match-detail 底部免责声明排版优化

- 用户截图反馈底部"本页所有 AI 分析内容均为模型基于历史数据的推演..."纯文本排版单薄。
- 改 `client/src/pages/match-detail/index.tsx:305`：把单行 Text 拆成 4 个节点 — 装饰条 + 标题"免责说明" + 正文 + 强调句"不构成任何投资或决策建议"。
- 改 `client/src/pages/match-detail/index.scss` 末尾追加 5 个样式类 `.detail-footer-tip/.detail-footer-tip-bar/.detail-footer-tip-body/.detail-footer-tip-title/.detail-footer-tip-text/.detail-footer-tip-emphasis`：浅灰渐变卡片 + 左侧 4px 渐变竖条 + 标题加粗带字距 + 正文 24px/line-height 1.7 两端对齐 + 强调句加粗微调。
- 设计取向：与顶部 `.risk-reminder` 黄色警示区分，底部用柔和的灰白渐变，定位"补充说明"而非"警告"。

## 14:27 — navigationBarTitleText 预测→分析

- 三处 navigationBarTitleText 替换：`pages/index/index.config.ts` "AI 足球预测"→"AI 足球分析"；`pages/match-detail/index.config.ts` "AI 预测"→"AI 分析"；`pages/ai-detail/index.config.ts` "AI 预测详情"→"AI 分析详情"。
- 注释和变量名里的"预测"（`utils/ad.ts`、`match-detail/index.scss:605`、`ai-detail/index.tsx:181`）暂未动，非用户可见。

## 13:42 — 微信小程序 6.1.5 合规违规通知

收到腾讯官方通知，小程序涉嫌违反《运营规范》6.1.5（赌博、竞猜、外围彩票、彩票预测服务）。

**触发风险点（按项目模块映射）**：
- `bets` 投注表 + `wallets` 钱包 + VIP 会员 → 赌博/变现
- `vote-history` 投票历史 + 用户参与预测 → 竞猜
- `match_odds` + `bookmakers`（Pinnacle/bet365 等 10 家博彩公司）→ 外围彩票
- AI 多模型预测比赛结果 → 彩票预测服务

**整改方案要点**（已与用户沟通）：
1. 24h 内下架所有"参与式"功能：钱包、投注、投票、赔率展示
2. 全量替换敏感关键词（押注/盘口/竞猜/中奖/兑奖 → 分析/观点/数据参考）
3. AI 预测改为"只读内容展示"模式，移除用户输入-回报闭环
4. 每个预测页加免责声明（仅供研究参考，不构成投注建议）
5. 小程序类目从"体育竞猜"改为"体育资讯/体育数据服务"
6. 走申诉流程：提交整改前后截图 + 承诺书

**长期方向**：业务定位要从"竞猜产品"挪到"AI 体育数据/内容"赛道——10 个 AI 模型 + 完整数据基建保留，剥掉博彩元素即可。具体改造方案文档待用户确认后输出。

**用户状态**：尚未开始整改（刚收到通知来咨询）。

## 14:1x — 前端整改完成（仅前端，后端不动）

按用户明确指令「前端按建议改、后端不动（部署在阿里云）」，已全量完成前端去竞猜化改造：
- 删除 `pages/vote-history`（竞猜历史/命中率页），并从 `app.config.ts` pages 移除
- `match-detail`：移除「投出你的预测」投票区 + 「观看视频解锁 AI 预测」广告三态机，改纯只读 AI 分析；结论标签中性化（主队占优/势均力敌/客队占优），AI 共识→AI 观点、综合信心→模型信心，加免责声明
- `leaderboard`：移除「人类排行」竞猜 tab 及渲染，清理无用 `UserAvatar`/`isValidImageUrl`/`topUsers`
- `profile`：移除「我的战绩」菜单入口与竞绩统计块，分享文案去「完成 X 场预测，快来参与」
- `index`：移除「人类预测总场次」「人机对决」，AI 共识→AI 观点、查看预测→查看分析、AI预测总数→AI分析总数
- 全局敏感词替换（预测→分析、投注/竞彩/彩票/赌博去除）；小程序标题 `AI 预测世界杯`→`AI 足球分析`；品牌 `AI PREDICTOR`→`AI ANALYST`；`about`/`disclaimer`/`contact`/`profile-setup`/`ai-detail` 同步中性化
- 整改清单与后续步骤文档：`docs/compliance/6.1.5-remediation.md`

验证：所有改动文件 IDE lint **0 错误**；可见 UI 无 `投注/竞彩/彩票/赌博/人机对决/竞猜/我的战绩/预测世界杯` 残留（仅剩代码注释）。

**后续待用户执行（非代码，关键）**：
1. 微信后台改小程序类目：「体育竞猜」→「体育资讯/体育数据服务」
2. 提交申诉：整改前/后截图 + 承诺书（强调已下架投注/竞猜/投票/解锁，现为纯 AI 足球内容分析）
3. VIP 徽标决策：已弱化「请续费」CTA，但 `profile` 仍展示 VIP 状态（后端 wallets/vip 未动）；若想彻底规避「变现」联想，建议隐藏 VIP 入口
4. 上传前本地 `npm run build` 跑一遍 Taro 编译确认打包无误

## 17:04 — match-detail 隐藏所有 AI 比分/分数

- 用户截图反馈：AI 综合分析与各个 AI 模型卡片中的比分、信心指数、进度条、备选参考需要隐藏。
- 改 `client/src/pages/match-detail/index.scss`：在"综合备选比分"注释区新增统一隐藏规则：
  - `.summary-score`、`.summary-card .pred-conf`（综合比分、综合模型信心+进度条）
  - `.pred-score-area`、`.pred-card .pred-conf`（单模型比分、单模型信心+进度条）
  - `.pred-alt-score`（综合/单模型备选参考）
- 保留 AI 观点（`summary-short-text`、`pred-result`）和分析文字（`summary-text`、`pred-analysis`）。
- 验证：`read_lints` 0 报错。

## 17:20 — sync_odds 频率改为每 6 小时

- 用户要求赔率同步从"每日 00:00"改为"每 6 小时"。
- 改 `server/app/core/scheduler.py`：`sync_odds` cron `hour=0` → `hour="*/6"`（0/6/12/18 点整），docstring 与启动日志同步更新。lint 0 报错。
- **已向用户提示关键坑**：`match_odds.snapshot_date` 为日期型，同日多次拉取会互相覆盖，每天只留 1 条最新值，不会累积 4 个日内快照。
- **待用户拍板**：若只为"更新最新值"则当前改动足够；若要"积累日内多快照走势"需改 schema（加 `snapshot_ts DATETIME` + 改唯一键 + `odds_service.py:161` 快照字段）。截至本次回复用户尚未决定。

## 17:22 — match_odds 扩展：支持日内多赔率走势点（每 6 小时 → 每天 4 点，后续可更多）

- 用户确认需要每天 4 个（及以后更多）赔率走势点。采用**小时桶 `snapshot_hour`(0-23)** 方案（而非裸 DATETIME），每小时一个赔率点，支持任意同步频率。
- 改动文件：
  1. `server/app/models/odds.py`：`MatchOdd` 加 `snapshot_hour: int`（NOT NULL, default/server_default 0）；唯一约束 `uk_match_odds` 由 6 列扩为 7 列（末尾加 `snapshot_hour`）。
  2. `server/app/services/odds_service.py`：`sync_all_upcoming_odds` 与 `sync_league_odds` 计算 `now_utc=datetime.now(UTC)` → `snapshot_date=now_utc.date()`、`snapshot_hour=now_utc.hour`；`sync_odds_for_match` 签名加 `snapshot_hour` 并写入 rows。
  3. `server/app/utils/prompt_builder.py`：`_format_odds` 取最新快照由 `max(o.snapshot_date)` 改为 `max((o.snapshot_date, o.snapshot_hour))`，文案显示 `快照 {date} {hour:02d}:00`。
  4. `server/app/api/v1/odds.py`（私密页）：`/match` 改按 `(snapshot_date, snapshot_hour)` 取最新桶；`/movement` 排序加 `snapshot_hour` 且返回点带 `hour`；`/backtest` 的 `odds_map` 与 `get_odd` 改用 `(date,hour)` 元组排序/比较。
  5. 新增迁移 `server/alembic/versions/r4s5t6u7v8w9_add_snapshot_hour_to_match_odds.py`：`down_revision='q2r3s4t5u6v7'`（确认是当前 head）；upgrade = 加列 + drop/recreate `uk_match_odds` 含 `snapshot_hour` + 建复合索引 `ix_match_odds_snapshot(date,hour)`；downgrade 反向。**部署需在阿里云跑 `alembic upgrade head` 应用**。
- 设计要点：同小时桶内多次拉取 upsert 覆盖（幂等）；跨小时桶自动追加 → 每 6 小时跑自然落 4 个不同小时桶 = 每天 4 个走势点；改每小时跑即 24 点，无需再动表。
- 验证：4 个 py 文件 `read_lints` 0 报错；新迁移 `py_compile` 通过。前端 `client/` 无 odds 调用（未动）；`server/app/static/odds.js`（私密页）走势图接收的 series 点现含 `hour` 字段，展示兼容（未改前端）。

## 20:36 — 迁移失败抢修：MySQL 唯一索引被外键占用 (1553)

- 现象：改完 `match_odds` 引用 `snapshot_hour` 后本地起后端，`GET /api/v1/odds/backtest` 报 `OperationalError (1054, "Unknown column 'match_odds.snapshot_hour'")` —— 因为没跑迁移、远程库还没列。
- **关键事实**：`.env` 的 `DATABASE_URL` 指向**远程阿里云 MySQL（47.101.167.103）**，不是本机。后端连远程；`alembic upgrade head` 也连远程（alembic 用 sync `DATABASE_URL_SYNC`/pymysql，所以能连；但 async asyncmy 在 Windows 上连远程会 `WinError 87`，检查脚本要用 sync 引擎）。
- 第一次 `alembic upgrade head` 失败：`OperationalError (1553, "Cannot drop index 'uk_match_odds': needed in a foreign key constraint")`。原因：`match_odds.match_id → matches.id` 外键（`fk_match_odds_match`）复用了 `uk_match_odds`（其首列是 `match_id`）作为支撑索引，直接 drop 该唯一索引外键就没索引了。
- **MySQL DDL 非事务**：第一次跑时 `add_column snapshot_hour` 已提交，但索引重建失败 → 列已存在、uk 仍是旧 6 列、alembic 版本号仍停在 `q2r3s4t5u6v7` 的半残状态。
- 修复：把迁移 `r4s5t6u7v8w9` 改写为**幂等 + 先建独立索引 `ix_match_odds_match_id(match_id)` 支撑外键 → drop/recreate 唯一索引(含 snapshot_hour) → 删掉临时独立索引**。重写后重跑 `alembic upgrade head` 成功（exit 0），验证：列含 snapshot_hour、uk 为 7 列、两个外键均在、版本号=`r4s5t6u7v8w9`、临时索引已清理。
- 教训（写进 MEMORY.md）：① 本项目 DB 是远程阿里云；② MySQL 删被外键复用的唯一索引前必须先建独立支撑索引；③ 迁移要写成幂等以应对 DDL 半残。

## 21:43 — 回滚公开 home 页；私密 odds 页新增 AI 预测展示

- 用户改主意：删除此前新建的公开 `/home` 页（浏览器直开首页），改为**直接在私密赔率页 `/internal/odds` 的比赛列表里展示 AI 预测**。
- **删除 home**：
  - `main.py` 回滚：移除 `/home` 静态挂载（`StaticFiles(directory=static/public)`）+ 根路径 `/` 重定向到 `/home/`。现仅保留 `/internal/odds` 挂载。
  - `server/app/static/public/` 整个目录已 `Remove-Item -Recurse -Force` 删除（已确认目录不存在）。
- **odds.py 后端新增 AI 数据**（复用 Prediction + AIModel 表，无需新表）：
  - 列表 `/matches`：每条比赛加 `predictions_summary`（按 Prediction.result 聚合 home_win/draw/away_win 票数 + total），供列表「AI 共识」列。
  - 详情 `/match/{id}`：加 `predictions[]`（JOIN AIModel 取 model_name/avatar_url；每条 result/score_home/score_away/confidence/analysis）。
- **odds.js + odds.css 前端**：
  - 比赛列表表格新增「AI 共识」列：彩色 chip 显示主胜/平/客胜票数（最高票高亮），无预测显示"暂无 AI 观点"。
  - 每行新增「AI 详情 ▾」展开行：点开异步拉 `/match/{id}`，渲染每模型预测卡片（头像/模型名/胜平负标签/比分/置信度/分析）+ 多数观点汇总。
  - 加 `escapeHtml` 防注入；CSS 用 `--accent`/`--red`/`--accent-2` 等既有变量，注释用英文避免 PowerShell 写 UTF8 中文乱码（已踩坑：用 here-string 写中文注释会乱码，改英文 + 字节级修复）。
- **校验**：`py_compile odds.py/main.py` + `node --check odds.js` 全通过（ALL_OK）；列表接口实测返回 `predictions_summary` ✅；详情接口 `predictions` 因当时后端未 reload 未实测，需用户重启后端后验证。
- 注：用户本次明确「自己重启后端」，故未代为重启进程。
- **🔧 uvicorn --reload on Windows 坑（2026-07-22 实战）**：
  - 现象：用户重启后 `/match/{id}` 仍不返回 `predictions` 字段；前端"整个电脑卡死"。
  - 根因：`netstat` 显示端口 8000 有 **两个 LISTENING**（新 reload 进程 40048 + 旧进程 51032）。Windows 上端口被占后，新进程 bind 失败但仍 LISTENING，所有请求落到旧进程 → 跑的是旧代码。
  - 叠加效应：`--reload` 模式 (watchfiles) 在 Windows + 远程 MySQL + async 组合下，watchdog 反复触发 reload，**CPU 100%**，用户直观感受"电脑卡死"。
  - 解决方案：平时开发**去掉 `--reload`**，手动 `Ctrl+C` + `uvicorn` 重启。或 `Get-NetTCPConnection -LocalPort 8000 -State Listen | ForEach-Object { taskkill /F /PID $_.OwningProcess }` 强制清场后再启动。

## 22:00 — AI 详情展开 UI 重做（编辑/数据报告型，frontend-design skill）

- **用户反馈**：AI 详情展开后"丑得不能用"——`.ai-analysis` 文字挤成单行不换行、模型卡片无视觉分隔、emoji 头像、整体像 Word 另存 HTML。
- **根因**：旧 `.ai-analysis` 缺 `word-wrap: break-word; overflow-wrap: anywhere;`，导致 grid 卡片被中英混排长文本撑爆成屏幕宽；`.ai-item` 边框/背景/圆角不显眼。
- **改 frontend-design 方案**：走「编辑/数据报告」方向，避免紫色渐变 AI 模板：
  - 共识头部：左侧 3px 渐变竖条 + Source Han Serif SC 衬线大标题（"多数模型倾向 *主胜*，多数比分 *2-1*"）+ 右上角 JetBrains Mono 等宽小字（"10 模型 · 一致度 100%"）+ 下方三色比例条。
  - 每个模型条目：编号 01-10 + 渐变方块头像（基于模型名首字母 fallback：中文取前 1-2 个汉字、英文取前两词首字母、单长词取前 2 字符）+ 模型名 + 胜平负 chip + 等宽比分 + 等宽置信度数字 + 渐变进度条。
  - 分析正文：行高 1.85 + 字号 13px + `text-align: justify; text-justify: inter-ideograph;` + 左侧 1px 细分割线 + **核心 bug 修复 `word-wrap: break-word; overflow-wrap: anywhere;`**。
  - 入场动画：0.32s 缓动 fade-in + 4px 下移。
  - 字体 fallback 全用系统字体：Source Han Serif SC / Songti SC / PingFang SC / 微软雅黑 / JetBrains Mono，**不依赖外部 CDN**。
- **改动文件**：
  - `server/app/static/odds.js`：`renderAiDetail` 整段重写为新结构（consensus-head + model-list），新增 `avatarInitials` 工具函数。
  - `server/app/static/odds.css`：替换 `.ai-detail-row > .ai-detail-cell` 起至 `.ai-analysis` 整段（约 67 行）为新设计（约 200 行）；保留 `.ai-consensus / .ai-chip / .ai-chip-major`（列表列还在用）。
- **校验**：`node --check odds.js` 通过；CSS 关键 class（`ai-consensus-head / model-list / model-item / model-avatar / model-analysis / word-wrap`）已写入。
- **用户操作**：杀 8000 端口旧/新进程 + 非 reload 模式启动 uvicorn + Ctrl+F5 强刷浏览器。\n\n## 22:08 — AI 详情 UI 二次简化（用户要"至少左对齐"）\n\n- **用户反馈**：「没看出来你改了什么」+「至少需要左边对齐吧」。\n- **第一轮根因**：`netstat` 看到 8000 端口被旧/新 uvicorn 双占 + `--reload` watchdog 吃 CPU。\n- **第二轮根因**：用户已重启后端、JS 也跑了新版（截图能看到 `01 02 03...` 编号、`10 模型 · 一致度 100%`、`DeepSeek/Qwen/Doubao/Claude/Gemini`），**但旧 CSS 还在生效**——HTML 引用 `odds.css`/`odds.js` 无版本号，浏览器 Memory Cache 锁死旧文件。\n- **修复 1（缓存）**：HTML 改 `<link rel=\"stylesheet\" href=\"odds.css?v=2\" />` + `<script src=\"odds.js?v=2\"></script>`，强制浏览器重新拉。\n- **修复 2（视觉太花）**：用户认为第一版（编辑/数据报告型，grid 5 列 + 衬线大标题 + 渐变竖条 + 68px 缩进分析）"丑"且"不是左对齐"。重写为**朴素左对齐列表版**：\n  - `.model-head`：`display: flex; flex-wrap: wrap`（不再 grid）\n  - `.model-item`：去掉背景/边框/圆角/hover 动效，只留 `border-bottom: 1px solid rgba(255,255,255,.05)` 分隔\n  - `.model-analysis`：`padding-left: 68px` 删除 + 删左侧分割线 ::before，文字从最左起\n  - `.ai-consensus-head`：去掉 grid 布局 + 衬线字体 + 左侧渐变竖条，改回普通 15px sans-serif\n  - padding 整体从 22/26 缩到 16/18；头像 30→22px；置信度 chip 简化\n- **校验**：`node --check odds.js` 通过；CSS 关键 class 全部就位；旧样式残留（`grid-template-columns: 26px`、`padding-left: 68px`）已 grep 确认清除。\n\n## 22:10 — 比赛列表表头吸顶（不被 AI 详情展开行撑爆）\n\n- **用户反馈**：「比如我展开某场比赛，上面的这一块应该是固定的，现在感觉是被撑开了」—— 红色圈圈的是表头那行（时间/主队/客队/.../操作）。\n- **根因**：`<th>` 已写 `position: sticky; top: 0`（CSS 213-222 行），但**外层 `.table-wrap` 没设 `max-height`，整页是 window 滚动**——sticky 元素在窗口滚动时跟着滚出视口，永远没机会触发 sticky 行为。展开 AI 详情后表格更高，**整张表把页面撑得很长**，表头自然就\"被撑开\"滚走了。\n- **修复**：只对 `#matchTableWrap`（比赛列表**唯一**容器，看板/回测用别的 wrap 不受影响）加：\n  - `max-height: calc(100vh - 240px)`（顶栏 60 + tab 50 + 过滤栏 80 + panel padding 40）\n  - `overflow-y: auto`：表格内独立滚动，不撑页面\n  - `thead th` 加 `z-index: 2; box-shadow` + `::after` 底线，吸顶时视觉清晰\n- **HTML cache-buster**：`?v=2` → `?v=3`（用户浏览器已缓存 v2，必须再升）。\n- **注意**：`<table>` 内 sticky th 是浏览器**支持**的，但要求**外层是滚动容器**——所以前面 th 的 `top: 0` 不生效的根因就是外层不滚动。\n- **校验**：grep 确认 195/201/208 行三个 CSS 块就位；HTML 引用 `?v=3`。\n\n## 22:12 — cache-busting 升级（改文件名 + no-cache meta）\n\n- **用户反馈**：「这不还是被撑开了吗」—— 截图里列名只显示 4 个（其他 5 列被横向滚出）、AI 详情内容是旧版（\"AI CONSENS...\" 截断 + 1./2./3. 编号 + 分析文字散在屏幕中右）。\n- **根因**：`?v=2` → `?v=3` 改了，**但用户浏览器 HTML 引用还是 `?v=2`**（HTML 也是缓存的）。这意味着浏览器根本不会去拉新 HTML，也就不会去拉新 CSS。\n- **真正的 cache-busting 必杀**：`odds.css` → `odds.v4.css`，`odds.js` → `odds.v4.js`。文件 URL 变了，浏览器**绝对不可能**用旧缓存（即使 HTML 本身被缓存，HTML 引用的 `odds.v4.css` 名字浏览器也从来没见过）。\n- **HTML 头部加 no-cache meta 兜底**（防 HTML 自身被缓存）：\n  ```html\n  <meta http-equiv=\"Cache-Control\" content=\"no-store, no-cache, must-revalidate, max-age=0\" />\n  <meta http-equiv=\"Pragma\" content=\"no-cache\" />\n  <meta http-equiv=\"Expires\" content=\"0\" />\n  ```\n- **教训**：`?v=N` cache-busting 在 Chrome/Edge 严格模式下可能因为 HTTP 缓存头（Cache-Control: max-age）继续命中缓存层。**改文件名才是 100% 必杀**。后续每次迭代都改文件名（如 `odds.v5.css`），不依赖用户强刷。
